actions:
  - id: action.damageJudge
    name: ダメージ判定
    ruby: だめーじはんてい
    type: basic

    request:
      trigger: direct
      speed: normal
      timing: damageJudge

    triggerCondition:
      event: actionResolved
      condition:
        actionId: action.block
        hasAttackerAndBlocker: true

    text:
      effect: ブロック済み戦闘のダメージ判定を行い、サイズ比較に基づいて敗北したユニットを墓地へ送る。

    effect:
      - judgeDamage: {}
