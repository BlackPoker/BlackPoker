#macro( renderAction $row $arg0 )
%%% Action %%%
%\vspace{1zh} %余白追加
\vspace{2mm} %余白追加
\hrule height 0.5mm depth 0mm width 66.5mm %罫線
\vspace{1mm} %余白追加
            #if(${arg0}=='all' || ${row.format.contains(${arg0})})
                #if($isExtraDoc)
{\small\bf ■ ${texFn.cnv(${row.actName})} {\scriptsize (${row.actType})}} %Actionタイトル
                #else
                #if(${row.format.contains('lite')})
                    #if(${arg0}=='lite')
{\small\bf ■ ${texFn.cnv(${row.actName})} {\scriptsize (${row.actType})}} %Actionタイトル
                    #else
{\small\bf [1] ${texFn.cnv(${row.actName})} {\scriptsize (${row.actType})}} %Actionタイトル
                    #end
                #elseif(${row.format.contains('std')})
{\small\bf [2] ${texFn.cnv(${row.actName})} {\scriptsize (${row.actType})}} %Actionタイトル
                #elseif(${row.format.contains('pro')})
{\small\bf [3] ${texFn.cnv(${row.actName})} {\scriptsize (${row.actType})}} %Actionタイトル
                #elseif(${row.format.contains('mast')})
{\small\bf [4] ${texFn.cnv(${row.actName})} {\scriptsize (${row.actType})}} %Actionタイトル
                #else
{\small\bf ■ ${texFn.cnv(${row.actName})} {\scriptsize (${row.actType})}} %Actionタイトル
                #end
                #end
            #end
            #if(${row.actName.length()} > 9)
\\ \null \hfill 
            #else
\hfill 
            #end
            #if(${row.actTime})
                #if($isExtraDoc)
{\scriptsize\bf @${texFn.cnv(${row.actTrigger})}-${texFn.cnv(${row.actSpeed})}-${texFn.cnv(${row.actTime})} }
                #else
{\footnotesize\bf @${texFn.cnv(${row.actTrigger})}-${texFn.cnv(${row.actSpeed})}-${texFn.cnv(${row.actTime})} }
                #end
            #end
            #if(${row.actCost})
                #if($isExtraDoc)
  {\scriptsize\bf | } {\scriptsize\bf \$ ${texFn.cnv(${row.actCost})}}
                #else
  {\footnotesize\bf | } {\footnotesize\bf \$ ${texFn.cnv(${row.actCost})}}
                #end
            #end

            #if(${row.actKey.trim()})
{\footnotesize\bf ★${texFn.cnv(${row.actKey})}}
            #end
            #if(${row.actUnit.trim()})
{\footnotesize\bf ★${texFn.cnv(${row.actUnit})}}
            #end

            #if(${row.actNote.trim()})
%特記事項
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除

${texFn.cnv(${row.actNote})}
            #end
            #if(${row.frame})
#if(!$isExtraDoc)
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除
{\bf（フレーム）}

${texFn.cnv(${row.frame})}
#end
            #end
            #if(${row.actActCond})
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除
{\bf（起動条件）}

${texFn.cnv(${row.actActCond})}
            #end
            #if(${row.actTriggerCond})
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除
{\bf（誘発条件）}

${texFn.cnv(${row.actTriggerCond})}
            #end
            #if(${row.actTarget})
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除
{\bf（対象）}

${texFn.cnv(${row.actTarget})}
            #end
            #if(${row.actEffect})
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除

{\bf（効果）}

${texFn.cnv(${row.actEffect})}
            #end
#end

#macro( renderCharacter $row $arg0 )
%%% Character %%%
%\vspace{1zh} %余白追加
\vspace{2mm} %余白追加
\hrule height 0.5mm depth 0mm width 66.5mm %罫線
\vspace{1mm} %余白追加
            #if(${arg0} == 'all' || ${row.format.contains(${arg0})})
                #if($isExtraDoc)
{\small\bf 〇 ${texFn.cnv(${row.charName})} {\scriptsize (${row.charType}) }} %Characterタイトル
                #else
                #if(${row.format.contains('lite')})
                    #if(${arg0}=='lite')
{\small\bf ■ ${texFn.cnv(${row.charName})} {\scriptsize (${row.charType}) }} %Characterタイトル
                    #else
{\small\bf [1] ${texFn.cnv(${row.charName})} {\scriptsize (${row.charType}) }} %Characterタイトル
                    #end
                #elseif(${row.format.contains('std')})
{\small\bf [2] ${texFn.cnv(${row.charName})} {\scriptsize (${row.charType}) }} %Characterタイトル
                #elseif(${row.format.contains('pro')})
{\small\bf [3] ${texFn.cnv(${row.charName})} {\scriptsize (${row.charType}) }} %Characterタイトル
                #elseif(${row.format.contains('mast')})
{\small\bf [4] ${texFn.cnv(${row.charName})} {\scriptsize (${row.charType}) }} %Characterタイトル
                #else
{\small\bf ■ ${texFn.cnv(${row.charName})} {\scriptsize (${row.charType}) }} %Characterタイトル
                #end
                #end
            #end
            #if(${row.charName.length()} > 9)
\\ \null \hfill 
            #else
\hfill 
            #end
            #if(${row.charKey})
{\footnotesize\bf ☆${texFn.cnv(${row.charKey})} }
            #end

            #if(${row.charSize})
{\bf サイズ : ${texFn.cnv(${row.charSize})} }
            #end

            #if(${row.charNote})
%特記事項
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除

${texFn.cnv(${row.charNote})}
            #end
            #if(${row.charLabel})
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除

{\bf（ラベル）}

${texFn.cnv(${row.charLabel})}
            #end
            #if(${row.charAbility})
\vspace{1mm}%余白削除
\hrule height 0.1mm depth 0mm width 66.5mm %罫線
\vspace{1mm}%余白削除

{\bf（能力）}

${texFn.cnv(${row.charAbility})}
            #end
#end
