.. _act-${data.id}:

#set($x = "-")
#foreach( $nestList in $actList)
    #if($foreach.count == 1)
.. _actionlist-${data.id}:

================================================================
#if(${arg1})${arg1}#endアクションリスト
================================================================

.. note::
   **表記について**

   - **@** はトリガー-スピード-タイミングの短縮表記です。（例: @直接-通常-クイック）

.. contents:: 目次
   :depth: 2
   :local:

    #end

----

${nestList.type}
======================================================
    #foreach( $row in ${nestList.acts})

        #if($foreach.count != 1)
----
        #end

.. _act-${row.actId}:

.. index::
   single: ${rstFn.indexEntry(${row.actName}, ${row.ruby}, "アクション")}

${rstFn.nameWithRuby(${row.actName}, ${row.ruby})} (${row.actType})
---------------------------------------------------
        #if(${row.format})
**フォーマット:**

.. csv-table::
   :header-rows: 1

   "ライト", "スタンダード", "プロ", "マスター"
   "#if(${row.format.contains('lite')})◯#else${x}#end", "#if(${row.format.contains('std')})◯#else${x}#end", "#if(${row.format.contains('pro')})◯#else${x}#end", "#if(${row.format.contains('mast')})◯#else${x}#end"

        #end

        #if(${row.frame})
**フレーム:**

.. csv-table::
   :header-rows: 1

#if($frames)
#set($frameHeader = "")
#set($frameRow = "")
#foreach($fr in $frames)
#if($foreach.count > 1)#set($frameHeader = "${frameHeader}, ")#set($frameRow = "${frameRow}, ")#end
#set($frameHeader = "${frameHeader}${fr.name}")
#if(${row.frame.contains(${fr.name})})#set($frameRow = "${frameRow}◯")#else#set($frameRow = "${frameRow}${x}")#end
#end
   ${frameHeader}
   ${frameRow}
#else
${rstFn.cnv(${row.frame})}
#end

        #end

        #if(${row.actTime})
**@** ${row.actTrigger}-${row.actSpeed}-${row.actTime}

        #end
        #if(${row.actKey.trim()})
**キーカード条件:** ${row.actKey}

        #end
        #if(${row.actUnit.trim()})
**キーユニット条件:** ${row.actUnit}

        #end
        #if(${row.actCost})
**コスト:** ${row.actCost}

        #end
        #if(${row.actNote.trim()})
**特記事項:** ${row.actNote}

        #end
        #if(${row.actActCond})
**起動条件:**  

${row.actActCond}

        #end
        #if(${row.actTriggerCond})
**誘発条件:**  

${row.actTriggerCond}

        #end
        #if(${row.actTarget})
**対象:**  

${rstFn.cnv(${row.actTarget})}

        #end
**効果:**  

${rstFn.cnv(${row.actEffect})}

        #if(${row.story})
**ストーリー:**  

${row.story}

        #end
        #if(${row.flavor})
**フレーバー:**  

*${row.flavor}*
            #if(${row.flavorSpeaker})

~ ${row.flavorSpeaker} ~
            #end
        #end


        #if(${row.since})
**導入バージョン:**  ${row.since}
        #end


        #if(${row.update})
**更新バージョン:**  ${row.update}
        #end


        #if(${row.devNote})
**補足:**

${row.devNote}
        #end

    #end
#end

#foreach( $nestList in $charList)
    #if($foreach.count == 1)
.. _characterlist-${data.id}:

================================================================
#if(${arg1})${arg1}#endキャラクターリスト
================================================================

.. contents:: 目次
   :depth: 2
   :local:

    #end

----

${nestList.type}
======================================================
    #foreach( $char in ${nestList.chars})

        #if($foreach.count != 1)
----
        #end

.. _char-${char.charId}:

.. index::
   single: ${rstFn.indexEntry(${char.charName}, ${char.ruby}, "キャラクター")}

${rstFn.nameWithRuby(${char.charName}, ${char.ruby})} (${char.charType})
---------------------------------------------------
        #if(${char.format})
**フォーマット:**

.. csv-table::
   :header-rows: 1

   "ライト", "スタンダード", "プロ", "マスター"
   "#if(${char.format.contains('lite')})◯#else${x}#end", "#if(${char.format.contains('std')})◯#else${x}#end", "#if(${char.format.contains('pro')})◯#else${x}#end", "#if(${char.format.contains('mast')})◯#else${x}#end"

        #end

        #if(${char.frame})
**フレーム:**

.. csv-table::
   :header-rows: 1

#if($frames)
#set($frameHeader = "")
#set($frameRow = "")
#foreach($fr in $frames)
#if($foreach.count > 1)#set($frameHeader = "${frameHeader}, ")#set($frameRow = "${frameRow}, ")#end
#set($frameHeader = "${frameHeader}${fr.name}")
#if(${char.frame.contains(${fr.name})})#set($frameRow = "${frameRow}◯")#else#set($frameRow = "${frameRow}${x}")#end
#end
   ${frameHeader}
   ${frameRow}
#else
${rstFn.cnv(${char.frame})}
#end

        #end

        #if(${char.charKey.trim()})
**ユニット条件:** ${char.charKey}

        #end
        #if(${char.charSize})
**サイズ:** ${char.charSize}

        #end
        #if(${char.charLabel})
**ラベル:** ${char.charLabel}

        #end
        #if(${char.charAbility})
**能力:**  

${rstFn.cnv(${char.charAbility})}


        #end
        #if(${char.story})
**ストーリー:**  

${char.story}

        #end
        #if(${char.flavor})
**フレーバー:**  

*${char.flavor}*
            #if(${char.flavorSpeaker})

~ ${char.flavorSpeaker} ~
            #end
        #end


        #if(${char.since})
**導入バージョン:**  ${char.since}
        #end


        #if(${char.update})
**更新バージョン:**  ${char.update}
        #end


        #if(${char.devNote})
**補足:**

${char.devNote}
        #end

    #end
#end


#foreach( $nestList in $fogList)
    #if($foreach.count == 1)
.. _foglist-${data.id}:

================================================================
#if(${arg1})${arg1}#endフォグリスト
================================================================

.. contents:: 目次
   :depth: 2
   :local:

    #end

----

${nestList.type}
======================================================
    #foreach( $fog in ${nestList.fogs})

        #if($foreach.count != 1)
----
        #end

.. _fog-${fog.fogId}:

.. index::
   single: ${rstFn.indexEntry(${fog.fogName}, ${fog.ruby}, "フォグ")}

${rstFn.nameWithRuby(${fog.fogName}, ${fog.ruby})} (${fog.fogType})
---------------------------------------------------
        #if(${fog.format})
**フォーマット:**

.. csv-table::
   :header-rows: 1

   "ライト", "スタンダード", "プロ", "マスター"
   "#if(${fog.format.contains('lite')})◯#else${x}#end", "#if(${fog.format.contains('std')})◯#else${x}#end", "#if(${fog.format.contains('pro')})◯#else${x}#end", "#if(${fog.format.contains('mast')})◯#else${x}#end"

        #end

        #if(${fog.frame})
**フレーム:**

.. csv-table::
   :header-rows: 1

#if($frames)
#set($frameHeader = "")
#set($frameRow = "")
#foreach($fr in $frames)
#if($foreach.count > 1)#set($frameHeader = "${frameHeader}, ")#set($frameRow = "${frameRow}, ")#end
#set($frameHeader = "${frameHeader}${fr.name}")
#if(${fog.frame.contains(${fr.name})})#set($frameRow = "${frameRow}◯")#else#set($frameRow = "${frameRow}${x}")#end
#end
   ${frameHeader}
   ${frameRow}
#else
${rstFn.cnv(${fog.frame})}
#end

        #end

        #if(${fog.fogKey.trim()})
**ユニット条件:** ${fog.fogKey}

        #end
        #if(${fog.fogAbility})
**能力:**  

${rstFn.cnv(${fog.fogAbility})}


        #end
        #if(${fog.story})
**ストーリー:**  

${fog.story}

        #end
        #if(${fog.flavor})
**フレーバー:**  

*${fog.flavor}*
            #if(${fog.flavorSpeaker})

~ ${fog.flavorSpeaker} ~
            #end
        #end


        #if(${fog.since})
**導入バージョン:**  ${fog.since}
        #end


        #if(${fog.update})
**更新バージョン:**  ${fog.update}
        #end


        #if(${fog.devNote})
**補足:**

${fog.devNote}
        #end

    #end
#end

#foreach( $nestList in $trumpList)
    #if($foreach.count == 1)
.. _trumplist-${data.id}:

================================================================
#if(${arg1})${arg1}#end切札リスト
================================================================

$trumpData.description

#if($trumpData.devNote)

**補足:**

$trumpData.devNote

#end

.. contents:: 目次
   :depth: 2
   :local:

    #end

----

${nestList.type}
======================================================
    #foreach( $trump in ${nestList.trumps})

        #if($foreach.count != 1)
----
        #end

.. _trump-${trump.trumpId}:

.. index::
   single: ${rstFn.indexEntry(${trump.trumpName}, ${trump.ruby}, "切札")}

${rstFn.nameWithRuby(${trump.trumpName}, ${trump.ruby})} (${trump.trumpType})
---------------------------------------------------
        #if(${trump.format})
**フォーマット:**

.. csv-table::
   :header-rows: 1

   "ライト", "スタンダード", "プロ", "マスター"
   "#if(${trump.format.contains('lite')})◯#else${x}#end", "#if(${trump.format.contains('std')})◯#else${x}#end", "#if(${trump.format.contains('pro')})◯#else${x}#end", "#if(${trump.format.contains('mast')})◯#else${x}#end"

        #end

        #if(${trump.frame})
**フレーム:**

.. csv-table::
   :header-rows: 1

#if($frames)
#set($frameHeader = "")
#set($frameRow = "")
#foreach($fr in $frames)
#if($foreach.count > 1)#set($frameHeader = "${frameHeader}, ")#set($frameRow = "${frameRow}, ")#end
#set($frameHeader = "${frameHeader}${fr.name}")
#if(${trump.frame.contains(${fr.name})})#set($frameRow = "${frameRow}◯")#else#set($frameRow = "${frameRow}${x}")#end
#end
   ${frameHeader}
   ${frameRow}
#else
${rstFn.cnv(${trump.frame})}
#end

        #end

        #if(${trump.trumpKey.trim()})
**ユニット条件:** ${trump.trumpKey}

        #end
        #if(${trump.trumpLabel})
**ラベル:** ${trump.trumpLabel}

        #end
        #if(${trump.trumpAbility})
**能力:**  

${rstFn.cnv(${trump.trumpAbility})}


        #end
#set($rel = $trumpRelations.get(${nestList.type}))
#if($rel)
#set($relActs = $rel.get("acts"))
#set($relChars = $rel.get("chars"))
#if($relActs.size() > 0 || $relChars.size() > 0)
**関連:**  

#if($relActs.size() > 0)
*アクション*

#foreach($relAct in $relActs)
* :ref:`${relAct.actName} <act-${relAct.actId}>`
#end

#end
#if($relChars.size() > 0)
*キャラクター*

#foreach($relChar in $relChars)
* :ref:`${relChar.charName} <char-${relChar.charId}>`
#end

#end
#end
#end
        #if(${trump.image} && !${trump.devNote})
**イメージ:**  
${trump.trumpName}のイメージ (:numref:`frame-${trump.trumpId}-image`)

.. _frame-${trump.trumpId}-image:
.. figure:: ../frame/images/${trump.image}.*
   :height: 200px
   
   ${trump.trumpName}のイメージ

        #end
        #if(${trump.story})
**ストーリー:**  

${trump.story}

        #end
        #if(${trump.flavor})
**フレーバー:**  

*${trump.flavor}*
            #if(${trump.flavorSpeaker})

~ ${trump.flavorSpeaker} ~
            #end
        #end


        #if(${trump.since})
**導入バージョン:**  ${trump.since}
        #end


        #if(${trump.update})
**更新バージョン:**  ${trump.update}
        #end


        #if(${trump.image} && ${trump.devNote})
.. list-table::
   :widths: 35 65
   :header-rows: 0
   :class: borderless

   * - .. _frame-${trump.trumpId}-image:
       .. figure:: ../frame/images/${trump.image}.*
          :height: 200px
          
          ${trump.trumpName}のイメージ
     - **補足:**

       ${trump.devNote}

        #elseif(${trump.devNote})
**補足:**

${trump.devNote}
        #end

    #end
#end